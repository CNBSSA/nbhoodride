# PG Ride Daily Audit — 2026-08-13

## Summary
- Overall: **RED**
- **develop ↔ main parity:** 0 commits on `develop` not in `main`, **1** on `main` not in `develop` (merge commit `da7140b` / PR #207 only); `git diff origin/develop origin/main` is **empty** (trees match).
- **Promote develop → main:** **N/A** — already aligned at application level; merge commit on `main` only.
- **Biggest risk to riders or drivers today:** Production health could not be verified from this run’s network probes (TLS reset on Railway and custom-domain URLs). GitHub **Daily Audit Gate** has not completed a full successful run on current `main` since PR #207 promote — the scheduled tripwire failed before job steps ran.

## Automated gates
- **audit:daily:** **FAIL** (exit 1) — `npm run check` **PASS**; `npm test` **PASS** (40 files, 175 tests); `smoke:production` **FAIL** (all production fetches: `fetch failed` / `ECONNRESET`).
- **check:** migration drift OK (60 tables); PWA assets OK; `tsc` OK.
- **test:** 175/175 passed (Vitest 4.1.9).
- **smoke:production:** **FAIL** — e.g. `GET /health`: fetch failed (`https://nbhoodride-production.up.railway.app/health`); 16 route/icon failures; warnings on `/health/ready`, screenshots, `pgride.com` / `pgride.app` DNS.
- **/health/ready:** **Not reached** — `TypeError: fetch failed`, cause `read ECONNRESET` (`scripts/daily-audit.mjs:71`).
- **External corroboration:** `openssl s_client` to `nbhoodride-production.up.railway.app:443` → `no peer certificate available`, handshake errno 104; HTTP to Railway IP with Host header returns `404` from `envoy` (edge up, HTTPS path not established from probe).
- **GitHub Actions “Daily Audit Gate”:** run **31696275713** (2026-08-13T11:36:46Z, schedule, `main` @ `da7140b`) → **failure** in ~2s, **zero job steps recorded** (job did not execute audit script). Prior post-promote run **31618493536** (2026-08-12 push on `#207`) same pattern. Last **success** on scheduled gate: **31592678556** (2026-08-12T11:37:27Z, pre-promote SHA `532b1b6`).

## Phase 3b — Scheduled & coworker (E / F)
- **E — Solo schedule:** **CODE-ONLY PASS** — traced `ScheduleRideModal.tsx` (`scheduledAt` ISO on schedule path); APIs `GET /api/rides/scheduled`, `GET /api/driver/scheduled-rides`, `POST /api/driver/rides/:rideId/claim`, `confirm-scheduled`, WS `new_scheduled_ride` / claim payloads in `server/routes.ts`; storage `claimScheduledRide` in `server/storage.ts`.
- **F — Coworker group (`PG-XXXXXX`):** **CODE-ONLY PASS** — `POST /api/rides/create-shared-schedule` rejects missing/future `scheduledAt` (`server/routes.ts` ~6610–6617); `maxSlots: 3`, join full at 409 (~6736); `applyGroupDiscount` 30% (~6794); group claim via `assignDriverToSharedScheduleGroup` (~4236); driver board dedupe for `shared_schedule` in `getOpenScheduledRides` (`server/storage.ts` ~1135).
- **Blockers or regressions:** None found in static trace on `main` @ `da7140b`. Live/manual E/F not run (no production access from agent pod; no test accounts this run).

## P0 — Fix today
- None **confirmed** with production evidence this run. If users report total outage, treat as **[INFRA] P0** until Railway `/health` returns 200 from a network outside this agent pod.

## P1 — User-blocking
- **[INFRA]** **Daily Audit Gate broken on `main` since promote:** GitHub Actions runs **31618493536** and **31696275713** failed with empty step lists (~2–4s), so automated production smoke has not run successfully on `da7140b`. Evidence: `gh run list --workflow=daily-audit.yml`.
- **[INFRA]** **Apex domain not serving the app:** `peoplegoverned.com` A → `3.33.251.168`, `15.197.225.128` (registrar parking); TLS handshake reset from probe. Users hitting apex see parking, not PG Ride. `www.peoplegoverned.com` CNAME → `ewxj4jd9.up.railway.app` → `69.46.46.98` but HTTPS also reset from this pod (same class as Railway URL probe).
- **[RIDER] [DRIVER] [ADMIN] [PAYMENT] [SAFETY]** Production readiness **unverified** this run — smoke and `/health/ready` unreachable from audit environment; founder should confirm live `/health` and `/health/ready` in browser or `workflow_dispatch` Daily Audit Gate.

## P2 — Degraded experience
- **[INFRA]** Smoke warnings: `https://pgride.com` and `https://pgride.app` DNS not resolving (`scripts/smoke-production.mjs`).
- **[INFRA]** `develop` is **1 commit behind** `main` (merge commit only); fast-forward merge `main` → `develop` keeps branch pointers tidy (no code diff).

## Backlog metrics
- Pending rider approvals: **N/A** (no read-only DB access this run)
- Pending driver approvals: **N/A**
- Stuck active rides: **N/A**
- Payment anomalies: **N/A**

## Recommended actions
| Priority | Action | Owner |
|----------|--------|-------|
| P1 | Re-run **Daily Audit Gate** via `workflow_dispatch` on `main`; if steps still empty, fix GitHub Actions runner/checkout failure and confirm `npm run audit:daily` passes including production smoke. | Track B (founder / ops) |
| P1 | Point **apex** `peoplegoverned.com` to Railway (or redirect apex → `www`); verify `PUBLIC_APP_URL` matches canonical HTTPS URL for email, guardian (`/guardian/:token`), and SOS links (`server/appUrl.ts`, `server/routes.ts`). | Track B |
| P2 | Merge `main` → `develop` to clear the merge-commit skew (trees already identical). | Track B |

## Areas checked clean
- **`main` codebase @ `da7140b`:** `npm run check`, `npm test` (175 tests), migration drift (60 tables).
- **Scheduled solo + coworker group logic** on `main` (static trace; Phase 3b E/F).
- **Git parity:** no file diff between `origin/develop` and `origin/main`.
- **No secrets** logged; no production data mutated.
