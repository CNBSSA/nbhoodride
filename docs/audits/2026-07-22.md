# PG Ride Daily Audit — 2026-07-22

## Summary
- Overall: **RED** (automated production smoke blocked in agent environment; local `check` + `test` green on live **`main`**)
- **develop ↔ main parity:** **0 ahead / 1 behind** on `develop` (main @ `cc81eed` includes PR #141 promote not yet on `origin/develop`)
- **Promote develop → main:** **N/A** — production already ahead; **NOT READY** to treat `develop` as integration until fast-forwarded from `main`
- Biggest risk to riders or drivers today: **Production health unverified from this run** (TLS `ECONNRESET` to Railway). **Custom domain split** — apex still on registrar parking; `www` CNAME changed to a different Railway hostname than the documented production service.

**Audit ref:** `main` @ `cc81eed0b4522cdf1b69f5c89ac3ad671799be57` (merge PR #141 — brand consistency + driver home redesign; follows auth fixes PR #140)

## Automated gates
- **audit:daily:** **FAIL** (exit 1)
- **check:** **PASS** — migration drift OK (59 tables), PWA assets OK, `tsc` clean
- **test:** **PASS** — 33 files, 154 tests
- **smoke:production:** **FAIL** — all HTTPS fetches to `https://nbhoodride-production.up.railway.app/*` failed with `fetch failed` / underlying **`read ECONNRESET`** (same pattern as 2026-07-19–21 cloud egress)
- **/health/ready:** **Not retrieved** — `daily-audit.mjs` aborted on `TypeError: fetch failed` at `/health/ready` (`scripts/daily-audit.mjs`, TLS `ECONNRESET`)
- **Manual curl (agent):** `curl https://nbhoodride-production.up.railway.app/health` → `(35) Recv failure: Connection reset by peer`

## Phase 3b — Scheduled & coworker (E / F)
- **E — Solo schedule:** **CODE-ONLY PASS** on `main`
  - `ScheduleRideModal.tsx` sets `scheduledAt` on create (lines ~238–265)
  - `GET /api/driver/scheduled-rides` → `getOpenScheduledRides` + `getDriverUpcomingRides` (`server/routes.ts` ~4105–4112)
  - Solo claim: `POST /api/driver/rides/:rideId/claim` → `storage.claimScheduledRide` (`server/routes.ts` ~4137)
  - Confirm: `POST /api/driver/rides/:rideId/confirm-scheduled` (`server/routes.ts` ~1940)
  - WS: `new_scheduled_ride`, `scheduled_ride_claimed`, `scheduled_ride_taken` on claim path (~4146–4162, ~6274–6295)
- **F — Coworker group (`PG-XXXXXX`):** **CODE-ONLY PASS** on `main`
  - Create requires future `scheduledAt` (`server/routes.ts` ~6218–6226); `maxSlots: 3` (~6242)
  - Join: `joinSharedGroupAsRider` — slot race via `claimScheduleSlot`, joiner inherits `group.scheduledAt` (~6380), 4th joiner rejected (~6344), 30% discount at 2 riders via `applyGroupDiscount` (~6391–6394)
  - Group claim: `assignDriverToSharedScheduleGroup` branch (~4130–4135)
  - Driver board dedupe: one row per `shared_schedule` group (`server/storage.ts` ~1101–1108)
- **Blockers or regressions:** None traced in code on `main`; **no live E2E** (no test accounts / no prod HTTP from agent)

## P0 — Fix today
- None identified from code review or reachable production signals this run.

## P1 — User-blocking
- **[INFRA]** Production smoke and `/health/ready` could not be validated from the daily agent environment — TLS **`ECONNRESET`** to `nbhoodride-production.up.railway.app` (`npm run audit:daily`, `curl`). **Founder / external monitor must confirm** deploy health, Stripe readiness (`0.5-stripe`), and public routes. *Evidence:* audit log above; not proof that production is down.
- **[INFRA]** **`peoplegoverned.com` apex DNS** still resolves to registrar parking (`dig +short peoplegoverned.com A` → `3.33.251.168`, `15.197.225.128`), not Railway. Users hitting apex get parking, not the PWA. *Evidence:* `dig` from audit host 2026-07-22.
- **[ADMIN]** **`develop` is 1 commit behind `main`** after promote — integration branch does not match live production until fast-forward. *Evidence:* `git rev-list --left-right --count origin/develop...origin/main` → `0	1`.

## P2 — Degraded experience
- **[INFRA]** **`www.peoplegoverned.com`** CNAME → `ewxj4jd9.up.railway.app` (A `69.46.46.98`) while **`nbhoodride-production.up.railway.app`** → `69.46.46.90` — possible wrong Railway service or stale custom domain target (changed since 2026-07-21 audit). HTTPS also **ECONNRESET** from agent to both hostnames.
- **[INFRA]** Phase 0 readiness **`customDomainConfigured()`** only treats `pgride.com` / `pgride.app` as pass, not **`peoplegoverned.com`** — if `PUBLIC_APP_URL` is correctly set to People-Governed, automated readiness still **warns** on `0.7-domain`. *Evidence:* `server/phase0Readiness.ts` lines 53–57, label ~201–204.
- **[SAFETY]** Guardian share URLs depend on **`PUBLIC_APP_URL`** (`server/routes.ts` ~7287). If ops uses Railway hostname while users bookmark `www`, links may be correct but **canonical domain mismatch** persists until DNS + env aligned. Not verified live this run.

## Backlog metrics
- Pending rider approvals: **N/A** (no read-only DB access)
- Pending driver approvals: **N/A**
- Stuck active rides: **N/A**
- Payment anomalies: **N/A**

## Recommended actions
| Priority | Action | Owner |
|----------|--------|-------|
| P1 | Run `curl -sS https://nbhoodride-production.up.railway.app/health/ready \| jq` (and `/health`) from founder network; confirm Railway last deploy green and Stripe check passes | Track B |
| P1 | Point **`peoplegoverned.com` apex** (and align **`www`**) to the **same** Railway service as production; verify app JSON at `/health`, not parking page | Track B |
| P1 | **`git checkout develop && git pull && git merge origin/main`** (or ff) so `develop` matches live `main` @ `cc81eed` | Track B |
| P2 | Extend `customDomainConfigured()` / Phase 0 labels for **`peoplegoverned.com`** | Track A |

## Areas checked clean
- Typecheck, migration drift (59 tables), PWA asset gate on **`main`**
- Full unit/integration suite: **154/154** tests
- Scheduled solo + coworker group **API/UI wiring** on `main` (Phase 3b E/F code trace)
- Recent promote: PR #141 — client branding/driver dashboard touchpoints only; no scheduled-ride logic regressions spotted in diff scope
