# PG Ride Daily Audit — 2026-08-16

## Summary
- Overall: **YELLOW**
- **develop ↔ main parity:** develop **0 ahead / 1 behind** main (merge commit `fc6e7ca` only); `git diff origin/develop origin/main` is **empty** (same tree)
- **Promote develop → main:** **N/A** — main already includes PR #209; sync `develop` to `main` for branch pointer hygiene
- Biggest risk to riders or drivers today: **apex `peoplegoverned.com` still resolves to registrar parking IPs**, so marketing links and apex traffic miss the Railway app; **`www.peoplegoverned.com` is correctly on Railway**

## Automated gates
- **audit:daily (this agent pod @ 23:02 UTC):** exit **1** — `npm run check` **PASS**, `npm test` **PASS** (40 files, 175 tests), **smoke:production FAIL** (TLS `ECONNRESET` to `https://nbhoodride-production.up.railway.app/*`; agent egress cannot reach Railway — known limitation)
- **GitHub Actions Daily Audit Gate** run [31943579242](https://github.com/CNBSSA/nbhoodride/actions/runs/31943579242) @ **2026-08-16T11:09:48Z** on `main`: **success** (~57s) — external tripwire **GREEN** for full `npm run audit:daily` including production probes earlier today
- **/health/ready:** not reachable from this agent pod; rely on GHA tripwire + manual curl from founder network

## Phase 3b — Scheduled & coworker (E / F)
- **E — Solo schedule:** **CODE-ONLY PASS** — `ScheduleRideModal.tsx` sets `scheduledAt`; driver claim via `POST /api/driver/rides/:rideId/claim` → `claimScheduledRide`; confirm `POST /api/driver/rides/:rideId/confirm-scheduled` in `server/routes.ts`
- **F — Coworker group (`PG-XXXXXX`):** **CODE-ONLY PASS** — `create-shared-schedule` requires future `scheduledAt`, `maxSlots: 3`; join via `join-schedule` / `joinSharedGroupAsRider` inherits `group.scheduledAt`, slot cap + `applyGroupDiscount` at 2 riders; group claim uses `assignDriverToSharedScheduleGroup`
- Blockers or regressions: none traced in code on `main` @ `fc6e7ca`

## P0 — Fix today
- *(none from code review; no DB/SOS access this run)*

## P1 — User-blocking
- **[INFRA] [RIDER] [DRIVER]** Apex **`peoplegoverned.com`** A records → `3.33.251.168`, `15.197.225.128` (not Railway); **`www.peoplegoverned.com`** CNAME → `ewxj4jd9.up.railway.app` → `69.46.46.98`. Users/bookmarks on apex cannot reach PG Ride. Evidence: `host peoplegoverned.com` / `host www.peoplegoverned.com` from audit runner.

## P2 — Degraded experience
- **[INFRA] [SAFETY]** If `PUBLIC_APP_URL` is set to apex domain before DNS fix, guardian/SOS share links from `resolveAppUrl()` (`server/appUrl.ts`) may point at a dead host — keep **`https://www.peoplegoverned.com`** or Railway URL until apex is fixed. Evidence: `server/routes.ts` guardian `shareUrl: ${baseUrl}/guardian/${token}`.
- **[INFRA] [ADMIN]** **develop** branch pointer lags **main** by one merge commit after promote (`3390c69` vs `fc6e7ca`); trees identical but confusing for agents/CI. Evidence: `git rev-list --left-right --count origin/develop...origin/main` → `0	1`.
- **[INFRA]** Intermittent **Daily Audit Gate** scheduled failures (~3s, no steps) e.g. runs 31881296439 (2026-08-15), 31696275713 (2026-08-13); use **workflow_dispatch** when tripwire is ambiguous.

## Backlog metrics
- Pending rider approvals: **N/A** (no read-only DB)
- Pending driver approvals: **N/A**
- Stuck active rides: **N/A**
- Payment anomalies: **N/A**

## Recommended actions
| Priority | Action | Owner |
|----------|--------|-------|
| P1 | Point **apex** `peoplegoverned.com` to Railway (ALIAS/ANAME or redirect to `www`) — match `www` CNAME target | Track B |
| P2 | Fast-forward **`develop`** to **`main`** after promote (`fc6e7ca`) | Track B |
| P2 | Set **`PUBLIC_APP_URL`** to working canonical host (`www.peoplegoverned.com` or Railway) until apex DNS fixed | Track B |
| P2 | Investigate flaky **Daily Audit Gate** ~3s failures; re-run workflow_dispatch after promotes | Track B |

## Areas checked clean
- `main` @ `fc6e7ca`: migration drift (60 tables), PWA assets, TypeScript check
- Vitest: 175/175 passed
- Scheduled + coworker ride APIs and WS types traced on `main` (Phases 3b E/F)
- External production smoke: **green** via GHA run 31943579242 today
- No code-fix draft PR opened (issues are DNS/ops, not application regressions)
