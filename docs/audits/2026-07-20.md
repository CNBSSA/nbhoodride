# PG Ride Daily Audit — 2026-07-20

## Summary
- Overall: **RED** (automated production smoke failed; live HTTP not verifiable from cloud agent)
- **develop ↔ main parity:** **0** commits on develop not in main, **1** on main not in develop (merge commit `d403348` — PR #135 promote; `develop` should fast-forward from `main`)
- **Promote develop → main:** **N/A** — main is ahead of develop by the promote merge only; sync `develop` from `main`, no new promote needed until integration work lands on `develop`
- **Biggest risk to riders or drivers today:** **[INFRA]** Cannot confirm production is up from this environment (TLS `ECONNRESET` to Railway and custom domain). **[INFRA]** Apex `peoplegoverned.com` still resolves to registrar parking IPs, not Railway — riders using the branded URL may not reach the app. Founder/external smoke required.

## Automated gates
- **audit:daily:** **RED** — exit code 1
- **npm run check:** **PASS** — migration drift OK (59 tables), PWA assets OK, `tsc` clean
- **npm test:** **PASS** — 33 files, 154 tests
- **smoke:production** (`scripts/smoke-production.mjs`, `BASE_URL=https://nbhoodride-production.up.railway.app`): **FAIL** — all GETs `fetch failed` / curl `ECONNRESET` during TLS handshake (evidence: `69.46.46.90:443`, errno -104)
- **/health/ready:** **Not reached** — `daily-audit.mjs` threw on fetch (same `ECONNRESET`)
- **/api/payment/config:** **Not reached** (audit aborted before probe)
- **Custom domain probe (partial):** DNS only — apex `peoplegoverned.com` → `3.33.251.168`, `15.197.225.128`; `www` CNAME → `hdjeq48f.up.railway.app` → `69.46.46.0` (distinct from prod hostname `69.46.46.90`). HTTPS to apex/www also **ECONNRESET** from cloud (may be egress + parking page combined)

## Phase 3b — Scheduled & coworker (E / F)
- **E — Solo schedule:** **CODE-ONLY PASS** on `main` @ `d403348`
  - Create/list/claim/confirm paths present: `ScheduleRideModal` → ride create with `scheduledAt`; `GET /api/rides/scheduled` (`server/routes.ts` ~4058); driver claim `POST /api/driver/rides/:rideId/claim` → `storage.claimScheduledRide` (~4104); `POST /api/driver/rides/:rideId/confirm-scheduled` (~1940); WS `new_scheduled_ride`, `scheduled_ride_claimed`, `scheduled_ride_taken` (~4113–4128)
- **F — Coworker group (`PG-XXXXXX`):** **CODE-ONLY PASS**
  - `POST /api/rides/create-shared-schedule` requires future `scheduledAt` (~6185–6192), `maxSlots: 3` (~6209); join uses `claimScheduleSlot` + inherits `scheduledAt: group.scheduledAt` (~6347); full group → 409 (~6311); group claim → `assignDriverToSharedScheduleGroup` (~4097–4102); `applyGroupDiscount` at 2+ riders (~6360)
- **Blockers or regressions:** None identified in static trace; no live journey test (no prod access / test accounts)

## P0 — Fix today
- None confirmed in code without live prod verification. If external smoke shows Railway down, treat as **[INFRA] P0** — all riders/drivers/admins offline.

## P1 — User-blocking
- **[INFRA] P1** — **Custom domain apex not on app:** `peoplegoverned.com` A records point to AWS parking (`3.33.251.168`, `15.197.225.128`), not Railway. Marketing/legal links and `BRAND.companyDomain` (`shared/branding.ts`) assume this host. Evidence: `dig +short peoplegoverned.com`.
- **[INFRA] P1** — **develop behind main by promote merge:** Integration branch at `01e24eb`, production at `d403348`. Risk of fixes based on stale `develop`. Evidence: `git rev-list --left-right --count origin/develop...origin/main` → `0 1`.
- **[RIDER] [DRIVER] [PAYMENT] P1 (unverified)** — Production smoke failed; Stripe readiness (`0.5-stripe`), login, signup, CSRF, and PWA manifest could not be checked live. Requires founder curl from outside cloud agent.

## P2 — Degraded experience
- **[INFRA] P2** — `www.peoplegoverned.com` targets `hdjeq48f.up.railway.app` (`69.46.46.0`) while default prod URL uses `nbhoodride-production.up.railway.app` (`69.46.46.90`) — possible split-brain if both are used publicly.
- **[INFRA] P2** — Smoke warnings: `pgride.com` / `pgride.app` DNS not resolving (from `smoke-production.mjs`).

## Backlog metrics
- Pending rider approvals: **N/A** (no read-only DB access)
- Pending driver approvals: **N/A**
- Stuck active rides: **N/A**
- Payment anomalies: **N/A**

## Recommended actions
| Priority | Action | Owner |
|----------|--------|-------|
| P0/P1 | From a non-restricted network: `curl -sS https://nbhoodride-production.up.railway.app/health` and `/health/ready`; confirm Railway service healthy and redeploy if down | Track B |
| P1 | Point **apex** `peoplegoverned.com` to Railway (or redirect apex → `www` on Railway); align `PUBLIC_APP_URL` with canonical HTTPS URL (`server/phase0Readiness.ts`, `server/appUrl.ts`) | Track B |
| P1 | Fast-forward **`develop`** from **`main`** after promote (`git checkout develop && git pull && git merge origin/main`) | Track B |
| P2 | Reconcile `www` Railway service vs `nbhoodride-production` if both should serve same deploy | Track B |

## Areas checked clean
- Local **TypeScript**, **migration drift**, **PWA asset** gates on `main`
- **154** unit/integration tests passing
- **Scheduled solo + coworker group** server routes, storage helpers, slot limits, and WS event types traced on `main`
- **Safety** routes present: `/api/emergency/start`, `/api/guardian/track/:token`, SPA `/guardian/:token`, `/emergency/:token` (`server/routes.ts`, `client/src/App.tsx`); share URLs use `resolveAppUrl` when configured
- **Stripe webhook** idempotency path: `POST /api/webhooks/stripe` + `storage.claimWebhookEvent` (`server/routes.ts` ~8341)
- Recent **main** promote (#135): resilient live trip stats, auth/queryClient error surfacing, crawler-visible `server/publicPages.ts` (Stripe/reviewer visibility — not a functional regression)

## Code fixes
- **No draft PR** — failures are infra/verification gaps from this environment, not a identified application bug on `main`.
