# PG Ride Daily Audit — 2026-07-19

## Summary
- Overall: **RED** (automated `audit:daily` exit 1 — production smoke and `/health/ready` unreachable from this environment; local `check` + `test` **GREEN**)
- **develop ↔ main parity:** `0` commits on develop not in main · **`4`** commits on main not in develop (merge commits from PRs #120, #122, #125, #127)
- **Promote develop → main:** **NOT READY** — integration branch is behind live `main`; fast-forward `develop` to `82adaf5` before the next fix cycle (see `docs/GIT_WORKFLOW.md`)
- **Biggest risk to riders or drivers today:** Public marketing domain still misconfigured (`peoplegoverned.com` apex on registrar parking; `www` CNAME may not target the same Railway service as production). Production HTTP/TLS health could not be confirmed from the cron environment (egress allowlist blocks Railway HTTPS).

## Automated gates
- **Repo / branch audited:** `main` @ `82adaf5ac9139318562c85caa16704cebe5b46ff`
- **`npm run check`:** PASS — migration drift OK (59 tables), PWA assets OK, `tsc` clean
- **`npm test`:** PASS — 33 files, **154** tests
- **`smoke:production`:** FAIL — all `GET` to `https://nbhoodride-production.up.railway.app/*` → `fetch failed` / TLS `read ECONNRESET` (`scripts/smoke-production.mjs`)
- **`/health/ready`:** FAIL — same `ECONNRESET` from `scripts/daily-audit.mjs` (could not read readiness JSON or `0.5-stripe` / `0.2-public-url` flags)

## Phase 3b — Scheduled & coworker (E / F)
- **E — Solo schedule:** **CODE-ONLY PASS** — traced `ScheduleRideModal` → ride create with `scheduledAt`; `GET /api/rides/scheduled`; driver board `GET /api/driver/scheduled-rides`; claim `POST /api/driver/rides/:rideId/claim` → `storage.claimScheduledRide`; confirm `POST /api/driver/rides/:rideId/confirm-scheduled`; WS `scheduled_ride_claimed` / `scheduled_ride_taken` in `server/routes.ts` (~4058–4107). No manual ride executed (no test accounts in audit env).
- **F — Coworker group (`PG-XXXXXX`):** **CODE-ONLY PASS** — `POST /api/rides/create-shared-schedule` requires future `scheduledAt` (~6156–6164); `maxSlots: 3` (~6180); join via `joinSharedGroupAsRider` with atomic `claimScheduleSlot`, inherited `scheduledAt: group.scheduledAt` (~6318), 4th joiner rejected at capacity (~6282); group claim via `assignDriverToSharedScheduleGroup` (~4068–4073); driver board dedupes one row per group in `storage.getOpenScheduledRides` (~1101–1108); UI copy in `SharedScheduleSheet.tsx`. No manual join flow run.
- **Blockers or regressions:** None found in static trace on `main`. New **MD-only pickup** enforcement (`shared/serviceArea.ts`, `rideWorkflowService.validateRideRequest`) applies to scheduled/coworker creates — DC/VA-origin pickups now rejected by design.

## P0 — Fix today
- None **confirmed** from this run (no DB access; production endpoints not reachable over HTTPS from cron).

## P1 — User-blocking
- **[INFRA] [RIDER] [DRIVER]** `peoplegoverned.com` **apex** still resolves to registrar parking IPs `3.33.251.168`, `15.197.225.128` (`dig +short peoplegoverned.com A`) — users without `www` hit a parking page, not PG Ride.
- **[INFRA] [RIDER] [DRIVER]** `www.peoplegoverned.com` CNAME → `hdjeq48f.up.railway.app` → **`69.46.46.0`**, while `nbhoodride-production.up.railway.app` → **`69.46.46.90`** — possible stale/wrong Railway custom domain target; marketing URL may not serve the production app even after TLS works.

## P2 — Degraded experience
- **[INFRA]** Cloud agent **egress restricted** (no `*.railway.app` / `peoplegoverned.com` on allowlist); `curl https://www.google.com` → connection reset — daily cron **cannot** validate live deploy; founder/external monitor must confirm `/health` and `/health/ready`.
- **[INFRA]** `develop` **4 commits behind** `main` — fix branches should rebase/ff from latest integration before new PRs.
- **[SAFETY]** Guardian share URLs use `resolveAppUrl()` (`server/appUrl.ts`, `server/routes.ts` ~7224) — if production `PUBLIC_APP_URL` is unset or wrong, links break; not verifiable without prod env read.
- **[ADMIN]** `/health/ready` check **`0.7-domain`** still treats only `pgride.com` / `pgride.app` as “custom domain configured” (`server/phase0Readiness.ts` ~53–57, ~200–206), not `peoplegoverned.com` — readiness dashboard understates domain work remaining.
- **[RIDER] [DRIVER]** Maryland-only **pickup** policy enforced offline (`shared/serviceArea.ts`, `server/rideWorkflowService.ts` ~165–172) — riders/drivers expecting DC/VA-origin trips will see `PICKUP_OUTSIDE_MD_MESSAGE` (intentional regulatory gate on latest `main`).

## Backlog metrics
- Pending rider approvals: **N/A** (no `DATABASE_URL` in audit environment)
- Pending driver approvals: **N/A**
- Stuck active rides: **N/A**
- Payment anomalies: **N/A**

## Recommended actions
| Priority | Action | Owner |
|----------|--------|-------|
| P1 | Point `peoplegoverned.com` apex + `www` at the live Railway service (align with production hostname / correct Railway domain ID); remove registrar parking A records | **Track B** (founder / DNS) |
| P1 | From outside this agent: `curl -sS https://nbhoodride-production.up.railway.app/health/ready` and Railway dashboard — confirm deploy healthy, Stripe `0.5-stripe` pass, set `PUBLIC_APP_URL` to canonical HTTPS URL | **Track B** |
| P2 | Fast-forward `origin/develop` to `main` (`82adaf5`) so integration matches production | **Track B** (git) / **Track A** can open chore PR if requested |
| P2 | Optional: extend `0.7-domain` readiness to recognize `peoplegoverned.com` | **Track A** (draft PR → `develop`) |

## Areas checked clean
- Typecheck, migration drift (59 tables), PWA asset gate
- Full unit/integration test suite (154 tests)
- Scheduled solo + coworker group API/UI paths on `main` (code trace E/F)
- Driver scheduled board group deduplication (`storage.getOpenScheduledRides`)
- Coworker join race handling (`claimScheduleSlot` / slot release on create failure)
- No code-fix draft PR opened (findings are DNS/ops/egress or intentional policy; no isolated code defect identified)
