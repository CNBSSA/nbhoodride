# PG Ride Daily Audit — 2026-08-11

## Summary
- Overall: **YELLOW**
- **develop ↔ main parity:** 0 commits on `develop` not in `main`, **1** on `main` not in `develop` (merge commit `532b1b6` only); `git diff origin/develop origin/main` is **empty** (identical trees).
- **Promote develop → main:** **N/A** — `main` already includes PR #205 (`532b1b6`). Fast-forward **`develop`** to `main` for pointer hygiene (see [GIT_WORKFLOW.md](../GIT_WORKFLOW.md)).
- Biggest risk to riders or drivers today: **apex domain `peoplegoverned.com` still on registrar parking IPs** (not the Railway app); riders using apex may fail TLS or see a non-app page. Production Railway app probed **GREEN in GitHub Actions** today; this cloud agent pod **cannot** reach Railway URLs (TLS `ECONNRESET` — egress limitation, not treated as prod outage).

## Automated gates
- **Local agent pod (`npm run audit:daily` on `main` @ `532b1b6`):** **RED** — `check` PASS, `test` PASS (40 files, 175 tests), **`smoke:production` FAIL** (all public routes `fetch failed` / `ECONNRESET` on `https://nbhoodride-production.up.railway.app`), **`/health/ready` unreachable** from pod.
- **GitHub Actions “Daily Audit Gate”** run [**31487309399**](https://github.com/CNBSSA/nbhoodride/actions/runs/31487309399) @ **2026-08-11T11:35:29Z** on `main` (schedule): **conclusion success** — independent signal that production smoke + readiness passed from CI.
- **`/health/ready`:** Not verified from this pod; CI success implies readiness probes passed ~11.5h before this cron run.

## Phase 3b — Scheduled & coworker (E / F)
- **E — Solo schedule:** **CODE-ONLY PASS** — traced `ScheduleRideModal` → ride create with `scheduledAt`; `GET /api/rides/scheduled`; `GET /api/driver/scheduled-rides`; `POST /api/driver/rides/:rideId/claim` → `claimScheduledRide`; `POST /api/driver/rides/:rideId/confirm-scheduled`; WS `new_scheduled_ride`, `scheduled_ride_claimed`, `scheduled_ride_taken` in `server/routes.ts`.
- **F — Coworker group (`PG-XXXXXX`):** **CODE-ONLY PASS** — `POST /api/rides/create-shared-schedule` requires future `scheduledAt`; `POST /api/rides/join-schedule` → `joinSharedGroupAsRider` with atomic `claimScheduleSlot`, inherits `scheduledAt: group.scheduledAt`, 30% discount at 2+ riders without double-discount; max 3 seats; group claim via `assignDriverToSharedScheduleGroup`; driver board dedupe in `getOpenScheduledRides` (`server/storage.ts` lines 1132–1138).
- Blockers or regressions: **No dedicated vitest** for shared_schedule / join-schedule (coverage gap only).

## P0 — Fix today
- *(None confirmed from code gates or CI; no DB access to validate live SOS/payment queues.)*

## P1 — User-blocking
- **[INFRA] P1** — Apex **`https://peoplegoverned.com`** resolves to **`15.197.225.128`** / **`3.33.251.168`** (registrar parking), not Railway; TLS handshake **reset** from audit pod (`curl` exit 35). **`www.peoplegoverned.com`** CNAME **`ewxj4jd9.up.railway.app`** → **`69.46.46.98`** (app path). Marketing/links using apex strand users off the app.

## P2 — Degraded experience
- **[INFRA] P2** — **`develop`** branch ref **1 commit behind `main`** (merge-only skew); trees match — sync branch pointers after promote.
- **[INFRA] P2** — Smoke warnings: **`https://pgride.com`** / **`https://pgride.app`** DNS not resolving (`scripts/smoke-production.mjs`).
- **[RIDER][DRIVER] P2** — Scheduled/coworker flows lack automated API tests (manual/CI smoke only).

## Backlog metrics
- Pending rider approvals: **N/A** (no read-only DB)
- Pending driver approvals: **N/A**
- Stuck active rides: **N/A**
- Payment anomalies: **N/A**

## Recommended actions
| Priority | Action | Owner |
|----------|--------|-------|
| P1 | Point **apex `peoplegoverned.com`** (and redirect apex → `www` or Railway) so public URLs match `PUBLIC_APP_URL` / guardian share links | Track B (founder/DNS) |
| P2 | After promote, **`git merge origin/main` into `develop`** (or FF) so integration branch pointer matches production | Track B |
| P2 | Add vitest coverage for `join-schedule` / group claim race (`claimScheduleSlot`) | Track A |

## Areas checked clean
- `npm run check` — migration drift OK (60 tables), PWA assets OK, `tsc` clean on **`main` @ `532b1b6`**
- `npm test` — 175/175 passed
- Phase 5 code paths — SOS/emergency routes present (`/api/emergency/start`, `/api/emergency/incident/:token`, admin acknowledge); `resolveAppUrl()` in `server/appUrl.ts`
- Phase 6 — Stripe webhook + readiness implemented in `server/index.ts` / `server/phase0Readiness.ts` (live Stripe status via CI only today)
- Recent **`main`** merge #205 — platform rate centralization, `server/featureFlags.ts` for Stripe-lean mode; removed client `fareCalculator.ts` (server-side pricing)
