# PGRide environment variables
# Copy this file to .env and fill in values for local dev only.
# In production, set these in Railway → Variables (never commit them).

# === Required for app to start ===
NODE_ENV=production
DATABASE_URL=postgresql://user:pass@host:5432/dbname     # Railway Postgres plugin auto-sets this
SESSION_SECRET=                                          # 64-char random hex; rotate periodically

# === Required for browser clients (CORS) ===
ALLOWED_ORIGINS=                                         # comma-separated list of allowed origins, e.g. https://pgride.com,https://app.pgride.com. If unset, CORS is fully disabled (origin:false) and cross-origin requests are rejected.
APP_URL=                                                 # canonical https URL of the deployed app, e.g. https://pgride.com. Used in email links. Falls back to RAILWAY_PUBLIC_DOMAIN.

# === Required for admin onboarding ===
SUPER_ADMIN_SETUP_TOKEN=                                 # 64-char random hex; secret URL token
SUPER_ADMIN_EMAIL=                                       # email of the user to auto-promote at boot. Defaults to thrynovainsights@gmail.com if unset.

# === Required for email (registration, password reset, ride notifications) ===
# The app uses Resend (https://resend.com) for transactional email.
RESEND_API_KEY=                                          # re_... API key from Resend dashboard. WITHOUT this, signup says "check your email" but no email is sent — you'll see a !!! banner at boot.
RESEND_FROM=noreply@pgride.app                           # verified sender address in your Resend account
# (legacy) Gmail SMTP settings — currently unused; kept for reference only.
GMAIL_USER=thrynovaproductions@gmail.com
GMAIL_APP_PASSWORD=                                      # Gmail app password (NOT account password)
GMAIL_FROM=thrynovaproductions@gmail.com

# === Required for web push notifications ===
# Generate locally with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:thrynovaproductions@gmail.com
VITE_VAPID_PUBLIC_KEY=                                   # Same as VAPID_PUBLIC_KEY (exposed to client)

# === Required for AI chat features ===
ANTHROPIC_API_KEY=                                       # console.anthropic.com → API Keys

# === Optional — Stripe payments (skip for initial launch) ===
STRIPE_SECRET_KEY=                                       # sk_test_... or sk_live_...; powers card storage, top-ups, and ride shortfall charges
VITE_STRIPE_PUBLIC_KEY=                                  # pk_test_... or pk_live_... — must match the secret key's mode
STRIPE_WEBHOOK_SECRET=                                   # whsec_...; from `stripe listen` or Stripe Dashboard → Webhooks. Required by /api/webhooks/stripe

# === Optional — Twilio (SMS notifications) ===
TWILIO_ACCOUNT_SID=                                      # ACxxxxxxxx... from Twilio console
TWILIO_AUTH_TOKEN=                                       # auth token paired with the SID
TWILIO_PHONE_NUMBER=                                     # E.164 sender number, e.g. +12025551234

# === Optional — Checkr (driver background checks) ===
CHECKR_API_KEY=                                          # Checkr API key. If unset, admin "approve driver" skips background-check trigger.
CHECKR_WEBHOOK_SECRET=                                   # HMAC secret for /api/webhooks/checkr signature verification

# === Optional — Google Cloud Storage for driver documents ===
GOOGLE_CLOUD_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=                          # path to service-account JSON
GOOGLE_CLOUD_STORAGE_BUCKET=

# === Auto-set by Railway (do not set manually) ===
# RAILWAY_PUBLIC_DOMAIN=<your-app>.up.railway.app
# PORT=<auto>
